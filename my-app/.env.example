# ============================
# Supabase Environment (Example)
# Copy this file to ".env.local" and fill in real values.
# Do NOT commit ".env.local".
# ============================

# Your Supabase project URL (Settings → API → Project URL)
REACT_APP_SUPABASE_URL=YOUR_PROJECT_URL_HERE
# e.g. https://abcde12345.supabase.co

# Your Supabase anon public key (Settings → API → Legacy API Keys → anon public)
REACT_APP_SUPABASE_ANON_KEY=YOUR_ANON_KEY_HERE
# e.g. eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# --- How to use ---
# 1) Make a copy for local use (can do as below or just copy paste):
#      cp .env.example .env.local
# 2) Paste your real values into .env.local.
# 3) Restart the dev server so CRA picks up the vars:
#      npm start
#
# In code (already wired in src/utils/supabase.js):
#   import { supabase } from './utils/supabase'
#   // supabase is now configured via env vars

